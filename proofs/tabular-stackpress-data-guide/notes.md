# Review Notes

## Round 2 — 2026-08-01 — Auto-resolved data contracts

- Changed: added transactional system-schema upgrade/rollback, collision-safe
  hidden JSON installation, failed-promotion preservation, unstructured
  copy/export, owning-role view publication with compiled definitions,
  authorized filtered export, idempotent jobs, outbox dispatch state, and
  structured page/MCP adapter denial. The rendered cards now show their actual
  dispositions instead of labeling every row an executable contract.
- Review next: the rendered twelve-contract disposition ledger and PGlite result
  signals, including all twelve production-translation rows.
- Feedback applied: reuse accepted product truth and Frozen Spec 00001 evidence
  where it already answers composite keys, drift, concurrency, authority, and
  import recovery.
- Simulated or deferred: network PostgreSQL, pools, live identity, production
  MCP transport, workers, retention operations, deployment, and backup remain
  target validations.
- Open: visible saved-view controls and visible job/dead-letter/admin state.
- Approval path: user acceptance permits the remaining visible-state design
  round; it does not authorize production tasks or schema deployment.
